# API Workbench Local HTTPBin Manual QA

Start here: `python .\awb-local-httpbin.py`, then follow `START-HERE-TEST-FLOW.md`.

This fixture is deterministic and local-only. It uses dummy values; do not replace them with real secrets.
